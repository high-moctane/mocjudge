{
  "name": "back",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npx tsc",
    "clean": "rm -rf ./dist/*",
    "start": "node dist/index.js",
    "dev": "npx tsc-watch --onSuccess \"npm run start\"",
    "lint": "npx eslint --max-warnings 0 src && npx prettier -c .",
    "fix": "npx prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "@typescript-eslint/eslint-plugin": "^5.43.0",
    "@typescript-eslint/parser": "^5.43.0",
    "eslint": "^8.27.0",
    "prettier": "2.7.1",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "fastify": "^4.9.2"
  }
}
